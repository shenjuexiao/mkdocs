# mkdocs-github-cn.yml
# github.com/shenjuexiao
# 20260728

# chat.deepseek.com/a/chat/s/b543e549-be0d-42fc-9200-58ab953940e5
# MkDocs 部署 GitHub Pages

# .github/workflows/mkdocs-github-cn.yml
name: mkdocs-github-cn

on:
  push:
    branches:
      # 当 main 分支有推送时触发
      - main  
    paths:
      # 只有 docs 文件夹或配置文件变更时才触发
      - 'docs/**'  
      - 'mkdocs.yml'
      - '.github/workflows/**'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install dependencies
        # 如果有其他插件，也一并安装
        run: pip install mkdocs mkdocs-material  

      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy --force